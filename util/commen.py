#codind=utf-8
class CommonUtil:

    #判断一个字符串是否在另外一个字符串中
    def is_contain(self,str_one,str_two):
        str_two=str(str_two)
        if str_one in str_two:
            return True
        else:
            return False










